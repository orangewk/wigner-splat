# K2Q: 2 次位相 Turán の quasipolynomial 拡張(重み (2,1))— wip

日付: 2026-08-09 / 著者: 本線 / status: **wip — v0.4: luna R2 受理(v0.3.1)+ 数値検証通過(9,568 配置
違反ゼロ)+ Sol 協力の R2′ 採用で C₀ 10⁷⁰ → 10⁴²・接続用の系(K2Q-wt / K2Q-aff)追加。
v0.4 差分は luna R3 再レビュー待ち**

> 位置づけ: [2026-08-08-quadratic-phase-turan-K2.md](2026-08-08-quadratic-phase-turan-K2.md)(定理 K2)の
> 拡張。閉包ドラフト §4.3.5.3 の三原子一遷移補題・分裂枝 3→2+1 が二原子 block(導来多項式因子付き)を
> 渡す先の前提部品。orange 裁定 2026-08-09: 三原子補題より先に本拡張を書く。
> epistemic status: 本文中に明記(証明済み / 検算済み事実 / 予想 / open を区別)。

## 1. 設定と主張

F(t) = P(t)e^{q₁(t)} + c₂e^{q₂(t)}(t 実変数、P ≢ 0 複素多項式 deg P ≤ 2、c₂ ≠ 0、
q₁ ≠ q₂ 複素 2 次多項式)。重み (2,1) = 閉包ドラフトの導来原子 deg P ≤ 2(m−1)、m = 2 に対応。
- 包絡: m₁(t) := log|P(t)| + Re q₁(t)(値域 [−∞,∞))、m₂(t) := log|c₂| + Re q₂(t)、
  V := max(m₁, m₂)(> −∞)。
- I 長さ 1、J ⊆ I 長さ ε ∈ (0,1]、Λ := max_j sup_I |q_j′|、A := sup_I V − sup_J V ≥ 0。
- (P1)′ 自明上界: |F| ≤ e^{m₁} + e^{m₂} ≤ 2e^{V} pointwise(P の零点では m₁ = −∞ として成立)。

**定理 K2Q(v0.4 ドラフト)**: sup_I|F| ≤ C₀ ε^{−4} e^{A+Λε} sup_J|F|(C₀ 絶対定数・明示的。
本稿の勘定では C₀ = 10⁴² で足りる — さらなる最適化は open、真値は ~10³ 級と推測)。
ε^{−4} は必要(§2)。base の周波数非依存性(Λ は指数の Λε のみ)は K2 と同じ要求 —
chaining C′ が kernel に要求する形((γ,κ) = (4,1))。
**周波数非依存性の構造的理由(本証明の核)**: 3 階以上の有限差分は 2 次多項式を厳密に消去する
ので、e^{Δᵏu} 型の量から位相 e^{p} が完全に脱落し、多項式 P̃ の零点データだけが残る(§5.2)。

## 2. 退化テスト(ε^{−4} の必要性と障害の registry — 検算済み)

**(T1) 衝突退化 ⇒ Γ ≥ 4(ε^{−4} は必要)**: p := q₁ − q₂ → 0 の族、p = τt²(τ → 0)で
P := −c₂·[e^{−p} の **t-次数 ≤ 2** 切断] = −c₂(1 − τt²) と選ぶ(luna R2 minor 対応 —
p の羃でなく t の次数で切る; τ²t⁴ 項は class 外なので落とす)。すると
F/e^{q₂} = c₂(e^{−p} − (1−p))e^{p}·… ~ c₂p²/2 = c₂τ²t⁴/2 — **実 4 次多項式**。
deg 4 Chebyshev 配置(J 中央、I 端)で比 ~ 648/ε⁴(補題 E4)、包絡は平坦(A, Λε → 0)。
⇒ γ = 4 未満は不可能。これは閉包側 sharp test (1−e^{τt²})² ~ τ²t⁴(Γ(3) ≥ 4)の (2,1) 版。

**(T2) 単一零点 dip(γ ≥ 3 の別機構)**: P = (t−x₀)²(実重根、x₀ ∈ I∖J、dist(x₀,J) = d)、
p := iπ − [2log(t−x₀) の J 中心での 2-jet]。J 上 u := log P + p − iπ = O(ε³/d³)(Taylor 剰余、
|(2log)‴| ≤ 4/d³)⇒ σ_H ~ ε³/d³。一方 t = x₀ で P = 0、H = 1。比 ~ d³/ε³ ≤ ε^{−3}(d ≤ 1)。
教訓: **J 上の平坦性は I へ延長できない**(u は零点で −∞ へ発散)— K2 場合 (c) の
「u 小 on I」戦略は零点近傍で構造的に破綻する。ただし比自体は ε^{−4} 予算内。

**(T3) 対称 2 零点(ε^{−4} 飽和の第 2 機構)**: 零点 z₁, z₂ を J 中心 t_J に対し対称
(z₂ = 2t_J − z₁、距離 d)に置くと u‴ = 2[(t−z₁)^{−3} + (t−z₂)^{−3}] は t_J で 0(奇対称)、
u の J 上平坦性は 4 階から: |u⁗| ~ 12/d⁴ ⇒ σ_H ~ ε⁴/d⁴。dip で |H| ~ 1 ⇒ 比 ~ d⁴/ε⁴、
d ~ 1 で **ε^{−4} を飽和**。⇒ C₀ε^{−4} が sharp 候補(T1 と独立の機構で一致 — 傍証)。

## 3. 正規化と基本不等式(K2 からの継承 — 証明済み)

H := F/(c₂e^{q₂}) = 1 + P̃(t)e^{p(t)}、P̃ := P/c₂、p := q₁ − q₂。
φ̃ := log|P̃| + Re p = m₁ − m₂(値域 [−∞,∞))。P̃ ≠ 0 の点で ψ̃ := arg(P̃e^{p}) の連続分岐。
(P2)′ |H| ≥ |e^{φ̃} − 1|。 (P3)′ |H| ≥ e^{φ̃}|sin ψ̃|。 (P4)′ cos ψ̃ ≥ 0 なら |H| ≥ 1。
(いずれも K2 の (P2)–(P4) と同一の初等計算。P̃ = 0 の点は e^{φ̃} = 0 として (P2)′ が |H| = 1 を返す。)
σ := sup_J|F|、σ_H := sup_J|H|、t₂ := argmax_J|H|。osc_J m₂ ≤ Λε(|m₂′| = |Re q₂′| ≤ Λ)。
**注意: osc_J m₁ ≤ Λε は成立しない**(log|P| の寄与)— K2 との第 1 の差異。以下の場合分けは
純指数原子 m₂ 側で正規化することでこれを回避する。

**補題 E4(deg 4 Chebyshev 外挿)**: 実多項式 g(deg ≤ 4)が長さ ε の J 上 |g| ≤ μ なら、
J を含む長さ 1 の I 上 |g| ≤ 648μ/ε⁴。
*証明*: affine で J → [−1,1]、I ⊆ {|x| ≤ 3/ε}。Chebyshev 極値性で |g(x)| ≤ μ|T₄(x)|、
|x| ≥ 1 で |T₄(x)| = |8x⁴−8x²+1| ≤ 8x⁴ ⇒ |g| ≤ 8μ(3/ε)⁴ = 648μ/ε⁴。∎

## 4. 場合 (a)(b): 深い相殺がない場合(証明済み)

**場合 (a) sup_J φ̃ ≥ 1**(quasipolynomial 項が J 上どこかで支配): t_a := argmax_J φ̃
(φ̃ は J 上上半連続 + 実解析的(−∞ 点除く)で sup 達成; sup_J φ̃ ≥ 1 > −∞ なので t_a は零点でない)。
(P2)′: |F(t_a)| = e^{m₂(t_a)}|H(t_a)| ≥ e^{m₂(t_a)}(e^{φ̃(t_a)}−1) ≥ (1−e^{−1})e^{m₁(t_a)}。
J 上の包絡: m₁(t) = φ̃(t) + m₂(t) ≤ φ̃(t_a) + m₂(t_a) + Λε = m₁(t_a) + Λε(φ̃ の最大性 +
osc_J m₂ ≤ Λε)。m₂(t) ≤ m₂(t_a) + Λε ≤ m₁(t_a) + Λε(φ̃(t_a) ≥ 1 > 0)。
⇒ sup_J V ≤ m₁(t_a) + Λε ⇒ σ ≥ 0.63 e^{−Λε}e^{sup_J V}。
(P1)′: sup_I|F| ≤ 2e^{A}e^{sup_J V} ≤ **3.2 e^{A+Λε} σ**。∎(a)
(K2 の場合 (a) より単純化された — 正規化を純指数原子側に取ったため。)

**場合 (b) sup_J φ̃ ≤ 1 かつ σ_H ≥ 1/10**: J 上 pointwise V ≤ m₂ + max(0, φ̃) ≤ m₂ + 1 より
sup_J V ≤ sup_J m₂ + 1 ≤ m₂(t₂) + Λε + 1。σ ≥ |F(t₂)| = e^{m₂(t₂)}σ_H ≥ (1/10)e^{sup_J V − 1 − Λε}。
(P1)′: sup_I|F| ≤ 2e^{A}e^{sup_J V} ≤ **55 e^{A+Λε} σ**。∎(b)(K2 場合 (b) と同一。)

## 5. 場合 (c): 深い相殺 sup_J φ̃ ≤ 1, σ_H < 1/10(v0.2 で証明完成 — 未レビュー)

### 5.1 継承する平坦性データ(証明済み — K2 (c1)(c2) と同一計算)

- (c1)′ (P2)′ ⇒ |e^{φ̃}−1| ≤ σ_H ⇒ |φ̃| ≤ 1.2σ_H on J。特に e^{φ̃} ≥ 0.88 > 0 ⇒
  **P̃ は J 上零点なし**、ψ̃ は J 上連続。
- (c2)′ (P4)′ ⇒ cos ψ̃ < 0、(P3)′ ⇒ |sin ψ̃| ≤ 1.14σ_H ⇒ 分枝 k₀ 一定で
  |ψ̃ − (2k₀+1)π| ≤ 1.8σ_H on J。
- u := φ̃ + i(ψ̃ − (2k₀+1)π)(= log(P̃e^{p}) の分枝 − iπ(2k₀+1))は J 上 |u| ≤ 3σ_H、
  H = 1 − e^{u}。

### 5.2 構造事実: 有限差分は 2 次位相を厳密に消去する(検算済み)

u は「複素 2 次多項式 + Σ_{i≤ν} log(t−z_i) + 定数」の族(z_i = P̃ の零点、ν = deg P̃ ≤ 2)。
u‴(t) = 2Σ_i(t−z_i)^{−3}、u⁗(t) = −6Σ_i(t−z_i)^{−4}。K2(ν = 0)では u‴ = 0。

**基本恒等式**: h > 0、基点 t、W := P̃e^{p} とする。Δ³_h u(t) := u(t+3h)−3u(t+2h)+3u(t+h)−u(t)
に対し(u の分枝は J 上連続に固定; 定数と 2 次多項式 p は Δ³ で消える)、
  **e^{Δ³_h u(t)} = P̃(t+3h)P̃(t+h)³ / (P̃(t+2h)³P̃(t)) = Π_i ρ₃(x_i)**、
  x_i := (t−z_i)/h、 ρ₃(x) := (x+3)(x+1)³/((x+2)³x)。
同様に e^{Δ⁴_h u(t)} = Π_i ρ₄(x_i)、ρ₄(x) := (x+4)(x+2)⁶x/((x+3)⁴(x+1)⁴)。
(検算: 恒等式は e^{u} = −W から純代数的に従い、e^{p} の寄与は Δ³p = Δ⁴p = 0 で厳密に脱落。
分枝の任意性は指数関数で消える。有効性の条件は 4(resp. 5)節点で P̃ ≠ 0 のみ — 場合 (c) では
(c1)′ が J 上の零点非存在を保証。)

**展開の検算値**: ρ₃(x) − 1 = (2x+3)/(x(x+2)³)(分子の計算: (x+3)(x+1)³ − x(x+2)³ = 2x+3)。
v := 1/x とおくと |λ₃(x) − 2v³| ≤ 12|v|⁴(λ₃ := log ρ₃ 主枝、|x| ≥ 100)。
ρ₄ − 1 の分子 **n₄ − d₄ = −6x⁴ − 48x³ − 140x² − 176x − 81**(deg 4・主係数 −6。検算:
主係数は e₄ 差 880 − 886 = −6、定数項 0 − 81、x 係数 256 − 432 = −176 — v0.2 の粗い展開を
luna R1 指摘で精密化)。(n₄−d₄)/d₄ = −6v⁴ + 48v⁵ + O(v⁶) より真の 5 次係数は 48 —
**|λ₄(x) + 6v⁴| ≤ 50|v|⁵(|x| ≥ 10⁴; λ₄ := log ρ₄ 主枝 — ρ₄ は 1 近傍なので主枝で良い)**。
(一様評価の内訳: |v| ≤ 10⁻⁴ で |ρ₄−1+6v⁴| ≤ 48.2|v|⁵(分母 (1+3v)⁴(1+v)⁴ 込みの粗い評価)、
|log(1+w)−w| ≤ |w|²(|w| ≤ 1/2)で w = ρ₄−1、|w|² ≤ 37|v|⁸ ⇒ 計 ≤ 50|v|⁵。)
(v0.2 の誤差定数 40 は偽(漸近比 48)— luna R1 [major] を受諾して修正。)
これらは u‴, u⁗ の主要項 2Σv³、−6Σv⁴ と整合。

### 5.3 場合 (c) の証明

**ν = deg P̃ = 0 の帰着(luna R1 [major] 対応)**: P̃ が非零定数なら F は定理 K2 の class
そのもの(定数は c₁ 係数に吸収)であり、K2 が 300ε^{−2}e^{A+Λε} ≤ 300ε^{−4}e^{A+Λε} を与えて
主張より強い(場合 (a)(b) 含め全体がこの帰着で処理される; q₁ − q₂ が定数の退化も K2 の
射程内)。**以下 ν ∈ {1, 2}**。ν = 1 では以降の Σ_i・R2 は v₂ := 0 として読む。

η := 3σ_H(= sup_J|u| の上界、§5.1)、h := ε/12、t* := J の左端。定数 **R₀ := 10³**、
**c₁ := 10⁻⁴⁰**(絶対定数; 選択理由は各所で明示。履歴: v0.2 の R₀ = 10⁴/c₁ = 10⁻⁵⁸ は
R2 吸収が閉じず(luna R1 [blocking])v0.3 で 10⁶/10⁻⁶⁸ に、v0.4 で R2′(Sol、定数 8)採用
により 10³/10⁻⁴⁰ に緩和)。σ_H = 0 なら F は実解析性より恒等 0 で自明。

**(c-ii) σ_H ≥ c₁ε⁴**: e^{m₂(t₂)} = |F(t₂)|/σ_H ≤ σ/(c₁ε⁴)。J 上 pointwise
V ≤ m₂ + max(0,φ̃) ≤ m₂ + 1.2σ_H ≤ m₂ + 0.12 より sup_J V ≤ m₂(t₂) + Λε + 0.12。
(P1)′: sup_I|F| ≤ 2e^{A}e^{sup_J V} ≤ 2e^{0.12}c₁^{−1}ε^{−4}e^{A+Λε}σ ≤ 2.3·10⁴⁰ε^{−4}e^{A+Λε}σ。∎

**(c-i) σ_H < c₁ε⁴**: 二分岐 — (SN)「ある零点が dist(z_i, J) ≤ R₀ε」/(FAR)「全零点が
dist(z_i, J) > R₀ε」。

**(SN) は起こらない(超近接零点の平坦性下界)**: dist(z₁, J) ≤ R₀ε とする。
基点 t を J の左半分 J₀(長さ 6h = ε/2)全域で動かす。|Δ³_h u(t)| ≤ 8η より
|Φ(t) − 1| ≤ 8ηe^{8η} ≤ 9η on J₀、Φ := Π_iρ₃(x_i(t))(基本恒等式 — 有理関数、分枝非依存)。
t ∈ J、dist(z₁,J) ≤ R₀ε より全 t ∈ J₀ で |x₁(t)| ≤ 12R₀ + 12 =: X₁ = 12012、
|d₃(x₁(t))| = |x₁||x₁+2|³ ≤ (X₁+2)⁴ ≤ (X₁+4)⁴ =: D₁ ≈ 2.1·10¹⁶。
X₂ := (7D₁)^{1/3} ≈ 5.3·10⁵ とし、ν = 2 のとき z₂ で細分岐:

- (SN-a) ν = 1、または ν = 2 で min_{t∈J₀, 0≤j≤3}|x₂(t)+j| ≥ X₂(z₂ 遠方):
  |2x₂+3| = 2|x₂+1.5| ≤ 3|x₂+1| ≤ 6|x₂+2| より |ρ₃(x₂)−1| = |2x₂+3|/(|x₂||x₂+2|³)
  ≤ 6/(|x₂||x₂+2|²) ≤ 6/X₂³ < 1/D₁ on J₀。よって
  |ρ₃(x₁) − 1| ≤ (|Φ−1| + |ρ₃(x₂)−1|)/|ρ₃(x₂)| ≤ (9η + 6/X₂³)/(1 − 6/X₂³) ≤ 10η + 7/X₂³
  = 10η + 1/D₁ on J₀(ν = 1 なら直接 |ρ₃(x₁)−1| = |Φ−1| ≤ 9η)。
  分子 V₁(t) := 2x₁(t)+3 = (2/h)(t − (z₁ − 1.5h)) は t の 1 次・主係数 2/h(実)。
  Re V₁ も主係数 2/h の実 1 次式なので sup_{J₀}|V₁| ≥ sup_{J₀}|Re V₁| ≥ (2/h)·(6h/2) = 6。
  一方 |V₁| ≤ (10η + 1/D₁)·D₁ on J₀ ⇒ 6 ≤ 10ηD₁ + 1 ⇒ **η ≥ 0.5/D₁ ≈ 2.4·10⁻¹⁷**。
- (SN-b) ν = 2 で z₂ も近い(上の min < X₂): min 達成の (t,j) から任意の (t′,j′) へ
  |x₂(t′)+j′| ≤ |x₂(t)+j| + |t′−t|/h + |j−j′| ≤ X₂ + 6 + 3 = X₂ + 9 ⇒
  |d₃(x₂(t′))| ≤ (X₂+9)(X₂+11)³ ≤ (X₂+11)⁴ =: D₂ ≈ 7.7·10²²(luna R1 [minor] 対応 —
  節点シフト込みの明示計算)。Φ − 1 の分子
  N(t) := n₃(x₁)(2x₂+3) + d₃(x₂)(2x₁+3)(n₃ := ρ₃ の分子多項式;
  恒等式 n₁n₂ − d₁d₂ = n₁(n₂−d₂) + d₂(n₁−d₁))は t の多項式で deg = 5、
  **主係数 = 4/h⁵(実・z₁, z₂ に非依存**; x⁵ 係数が n₃ 側 2 + d₃ 側 2 — 検算済み)。
  Re N は主係数 4/h⁵ の実 deg 5 多項式なので Chebyshev 下界より
  sup_{J₀}|N| ≥ sup_{J₀}|Re N| ≥ (4/h⁵)·2·(6h/4)⁵ ≥ 60(実部経由 — luna R1 の簡潔化を採用)。
  一方 |N| = |Φ−1|·|d₃(x₁)d₃(x₂)| ≤ 9ηD₁D₂ ⇒ **η ≥ 60/(9D₁D₂) ≈ 4.2·10⁻³⁹**。
どちらでも η ≥ 4.2·10⁻³⁹。ところが (c-i) では η = 3σ_H < 3c₁ε⁴ ≤ 3·10⁻⁴⁰ < 4.2·10⁻³⁹ —
**矛盾**(c₁ の選択理由 0)。∎(SN)

**(FAR) 全零点 dist > R₀ε での外挿**: |x_i(t*)| ≥ R₀ε/h = 12R₀。v_i := 1/x_i(t*)、
M := max_i|v_i| ≤ 1/(12R₀)。基本恒等式は e^{Δᵏu} = Πρ_k を与えるので直ちには
Δᵏu − Σ_iλ_k(x_i) ∈ 2πiℤ までだが、|Δᵏu| ≤ 16η < 10⁻³⁸ < π かつ
|Σλ_k| ≤ Σ(2|v_i|³ + 12|v_i|⁴) < 10⁻¹¹ < π なので整数は 0 と確定(luna R2 minor 対応。
λ₄ の適用域 |x| ≥ 10⁴ は |x_i| ≥ 12R₀ = 1.2·10⁴ で満たされる)。
よって Δᵏu = Σ_iλ_k(x_i)(k = 3,4)であり、§5.2 の展開から:
  |Σ_i v_i³| ≤ (8η + 24M⁴)/2 = 4η + 12M⁴、 |Σ_i v_i⁴| ≤ (16η + 100M⁵)/6 ≤ 2.7η + 17M⁵。
**剛性補題 R2′**(2 複素数の同時羃和消滅 — Sol 提案 2026-08-09、検算済み):
v₁,v₂ ∈ ℂ、δ₃ := |v₁³+v₂³|、δ₄ := |v₁⁴+v₂⁴| ⇒ **max|v_i|³ ≤ 8(δ₃ + δ₄^{3/4})**。
*証明*: WLOG M' := |v₁| = max、r := v₂/v₁、|r| ≤ 1。|1+r³| < 1/8 かつ |1+r⁴|^{3/4} < 1/8
(⇒ |1+r⁴| < 1/16)と仮定すると、恒等式 1+r⁴ = r(1+r³) + (1−r) より
|1−r| ≤ |1+r⁴| + |r||1+r³| < 3/16。よって |1−r³| = |1−r||1+r+r²| ≤ 3|1−r| < 9/16、
ゆえに |1+r³| = |2−(1−r³)| > 23/16 — 矛盾。したがって |1+r³| ≥ 1/8(⇒ M'³ ≤ 8δ₃)
または |1+r⁴| ≥ 1/16(⇒ M'⁴ ≤ 16δ₄ ⇒ M'³ ≤ 8δ₄^{3/4})。∎
(v0.3 の R2(定数 4000、根分離場合分け)を置換 — R₀ = 10⁶ → **10³** に緩和できる。)
吸収の検算: M ≤ 1/(12R₀) = 8.4·10⁻⁵ ⇒ M^{3/4} ≤ 8.7·10⁻⁴。
8·12M⁴ = 96M³·M ≤ 0.008M³; 8·(17M⁵)^{3/4} = 8·17^{3/4}·M³·M^{3/4} ≤ 0.06M³
(δ₄ = 2.7η + 17M⁵ — λ₄ 誤差 50|v|⁵ 込みで |Σv_i⁴| ≤ (16η + 100M⁵)/6)。
⇒ 0.93M³ ≤ 8(4η + 2.1η^{3/4}) ⇒ M³ ≤ 53η^{3/4} ⇒ **M ≤ 4η^{1/4}**(η ≤ 1)。
**深い平坦性は零点を絶対距離へ押し出す**: η < 3c₁ε⁴ より
dist(z_i, t*) = h/|v_i| ≥ (ε/12)/(4η^{1/4}) ≥ 1/(48·(3c₁)^{1/4}) ≈ 1.6·10⁸ ≥ 3
(c₁ の選択理由 1; (3·10⁻⁴⁰)^{1/4} ≈ 1.3·10⁻¹⁰)。よって全零点は I から距離 ≥ 2。
w_i := 1/(t*−z_i)、|w_i| ≤ 1/3、級数
  u(t) = (deg ≤ 2 部) + Σ_{k≥3}(−1)^{k−1}s_k(t−t*)^k/k、 s_k := Σ_i w_i^k
は I 全域(|t−t*| ≤ 1)で収束し u の解析接続と一致(収束半径 ≥ 3)。|w_i| = 12|v_i|/ε ≤ 48η^{1/4}/ε。
- 尾部(k ≥ 5)on I: ≤ 0.6·48⁵·η^{5/4}/ε⁵ = 1.6·10⁸·η^{1/4}/ε·(η/ε⁴)
  ≤ 1.6·10⁸·(3c₁)^{1/4}·(η/ε⁴) ≤ 0.02·η/ε⁴(c₁ の選択理由 2; η^{1/4} < (3c₁)^{1/4}ε)。
- Q₄ := deg ≤ 4 切断。J 上(|t−t*| ≤ ε): |Q₄| ≤ η + 0.6(48η^{1/4})⁵ ≤ η(1 + 0.02) ≤ 1.02η。
  補題 E4(実部・虚部それぞれ、係数 2): sup_I|Q₄| ≤ 1300·1.02η/ε⁴ ≤ 1326η/ε⁴。
- 合算: sup_I|u| ≤ 1327·η/ε⁴ ≤ 1327·3c₁ ≤ 10⁻³⁶ ≤ 0.01 ⇒
  sup_I|H| = sup|1−e^{u}| ≤ 1.01·sup_I|u| ≤ 1341η/ε⁴ = 4023·σ_H/ε⁴ ≤ 4100σ_H/ε⁴。
仕上げ(K2 (c-i) と同一): sup_J V ≤ m₂(t₂) + Λε + 0.12 より
sup_I|F| ≤ e^{sup_I m₂}·sup_I|H| ≤ e^{A + sup_J V}·4100σ_Hε^{−4}
 ≤ 4100·e^{0.12}·ε^{−4}e^{A+Λε}·e^{m₂(t₂)}σ_H ≤ **4700 ε^{−4}e^{A+Λε}σ**。∎(FAR)(c)

**定理の総括**: C₀ = max(3.2, 55, 300(ν=0), 2.3·10⁴⁰, 4700) ≤ **10⁴²**(全分岐明示的)。∎ 定理 K2Q

### 5.4 証明の検証ポイントと残作業

- 整合性チェック(自前検算済み): T1(衝突)は (c-ii)/場合(b) 側、T2 は η ~ ε³ ≥ c₁ε⁴ で (c-ii)、
  T3(d ≥ 3)は η ~ ε⁴/81 ≥ c₁ε⁴ で (c-ii) — 剛性補題の「深平坦 ⇒ 零点遠方」と無矛盾。
  chirp: ν = 0 で u は 2 次多項式、(FAR) が E4 経由で自明に閉じる(K2 に退化)。
- **luna R1 レビュー(2026-08-09)の結果**: 定理への反例構成は失敗(複素共役根・対称根・
  SN 境界・deg P = 1・p 定数退化・ε→1・高 winding で探索)。基本恒等式・SN-b 主係数 4/h⁵・
  R2 場合分け・E4 複素化・regime 網羅性は検算済み判定。[blocking] R2 吸収(R₀ = 10⁴ では
  4000·14^{3/4}M^{15/4} ≈ 4.5M³ で破綻)、[major] λ₄ 定数(真値 48)、[major] ν = 0 未処理、
  [minor] D₂ 根拠・λ₄ 主枝 — **全て受諾し v0.3 で修正**(R₀ = 10⁶、c₁ = 10⁻⁶⁸、
  n₄−d₄ 全係数明示、ν = 0 の K2 帰着、D₂ = (X₂+11)⁴ の節点シフト計算、主枝明記)。
- **luna R2(2026-08-09)**: **受理可能判定 — blocking/major ゼロ**。FAR/SN/(c-ii)/λ₄ の全定数連鎖
  を末端まで再検算通過(SN 厳密値 η ≥ 4.21·10⁻⁶⁷ は本文値より安全側)。minor 3 件は v0.3.1 で補記済み。
- **数値検証(2026-08-09、v0.3.1)**: 9,568 配置(ランダム 8,000 + T1 48 + T2/T3 + chirp 1,500、
  grid 6001×3001)で正規化比 sup_I|F|·ε⁴/(e^{A+Λε}sup_J|F|) の**違反ゼロ**。最大 2.52(T2 族、
  ε = 0.1)、ランダム最大 0.79、chirp 最大 1.1·10⁻⁴(周波数非依存性の実証)。T1 族は
  ε = 0.05/0.1/0.2 で比 ≈ 1.0 に一様に張り付き — **ε⁻⁴ スケーリングの飽和(sharp 性)を確認**。
  スクリプト: セッション scratchpad k2q_numeric.py(プロトコルは K2 と同一)。
- **残作業(定理の成立には影響しない)**: 定数の最適化のみ(現状 c₁ = 10⁻⁶⁸ 起点で C₀ ~ 10⁷⁰。
  SN 分岐の X₂ 閾値と R2 定数の精密化で数桁級で縮む見込み。経験値は最大 2.52 — 真の C₀ は
  ~10³ 級以下と推測)。

## 6. 接続用の系と降下スキームへのインターフェース(v0.4 — Sol 協力 2026-08-09 を検算の上採用)

### 6.1 系 K2Q-wt(包絡正規化版)と K2Q-aff(affine 版)

U_F(t) := max(log|P(t)| + Re q₁(t), log|c₂| + Re q₂(t))(= V)、ノルムは L^∞ とする。

**系 K2Q-wt**: K 長さ L、E ⊆ K 長さ ℓ の区間、F = Pe^{q₁} + c₂e^{q₂}(deg P ≤ 2)に対し
  **‖e^{−U_F}F‖_{∞,K} ≤ C₂₁(L/ℓ)⁴‖e^{−U_F}F‖_{∞,E}**(C₂₁ = C₀ で可)。
*証明は本文の場合分けの正規化読み替えで済む(検算済み)*: G := e^{−U_F}|F| は (P1)′ より
G ≤ 2 on K。下界: (a) G(t_a) ≥ 1−e^{−1}(t_a で U = m₁)/(b) G(t₂) ≥ σ_H e^{−1} ≥ 1/(10e)
/(c-ii) G(t₂) ≥ e^{−0.12}c₁(ℓ/L)⁴ /(FAR) sup_K G ≤ sup_K|H| ≤ 4100(L/ℓ)⁴σ_H、
sup_E G ≥ e^{−0.12}σ_H /(ν=0)K2 の正規化版。**Λε 項は正規化版では現れない**(osc 評価は
非正規化 sup の比較にのみ必要だった)。

**系 K2Q-aff**: 同じ設定で A_{K,E} := sup_K U_F − sup_E U_F、Λ_K := max_j sup_K|q_j′| に対し
  **‖F‖_{∞,K} ≤ C₂₁(L/ℓ)⁴ e^{A_{K,E} + ℓΛ_K}‖F‖_{∞,E}**。
*証明*: 本定理の affine 変換(t ↦ Lt で 2 次多項式 class・deg P ≤ 2 は保存、Λ̃ε̃ = ℓΛ_K)。∎

### 6.2 (W,d,Δ) 降下との接続仕様(閉包 §4.3.5.3 向け・Sol 検証済みの読み)

- **ノルム**: 中心命題の ‖·‖_E は当面 **L^∞(E) と明記**する。L² への橋(Nikolskii 型
  sup_J ≲ |J|^{−1/2}‖·‖_{L²(J)})は本 class 用の補題が別途必要(実質 F1 と同種 — open)。
  下流の Fock 接続は従来どおり radial pointwise envelope 経由。
- **ε の読み**: ε_{K2Q} = |J_ρ|/|I_parent|。分裂枝の帳簿は
  ‖Q‖_I/‖Q‖_{J_ρ} = (‖Q‖_I/‖Q‖_{I_s})·(‖Q‖_{I_s}/‖Q‖_{J_ρ}) ≲ s^{−4}(s/ρ)^{−4}… の
  **telescoping で ρ^{−4}**(各段を粗く ρ^{−4} と数えて ρ^{−8} にしない)。
- **W 帳簿**: (2,1) block は W = 3、d = 2、Δ = 1 − r_B(r_B ∈ {0,1} = pair block の valuation)。
  r_B = 0 は K2(指数 2)、r_B = 1 は K2Q(指数 4)— **r_B は wrapper 側で metadata として
  保持**(次数から復元しない)。q_B − q₃ が定数なら係数へ吸収し d = 1 に正規化してから帰納。
- **B2 の守備範囲の縮小**: (2,1) への reduction 後は実分離・虚分離とも K2Q が周波数一様に
  処理する。B2 に残るのは「二項 quasipolynomial に落ちる前の多 block 段階」のみ。

### 6.3 radial chaining(従来メモ)

C′ は kernel を (γ,κ) 抽象で受けるため、kernel(2,1) = (γ,κ) = (4,1) として L2a′ の (2,1) 版が
従う。ray 制限 f(se^{iθ}) は P(se^{iθ}) が s の deg ≤ 2 多項式になるので class は閉じている
(検算済み)。

### 6.4 一般化の射程(Sol D 節の整理 — 将来用)

- 単一原子 Pe^{q}(deg P = ν 任意)には e^{Δᵏ log W} = Πρ_k(x_ℓ)(ρ_k(x) = Π_j(x+j)^{(−1)^{k−j}C(k,j)})
  が k ≥ 3 で成立し、s_k = 0(k = 3..ν+2)⇒ v = 0(Vandermonde)の定量版で
  binary transition P e^{q} + c e^{q′} は任意 ν へ拡張見込み(予想 Remez 指数 ν+2)。
- 一般 c の和には log の加法性がなく直接不可(例: 1 + ωe^{τt} + ω²e^{2τt}、ω = e^{2πi/3} は
  pairwise に還元できない三項同時衝突)。有限差分は cluster tree の**各 binary node の局所 kernel**
  という位置づけ(Sol の整理を採用)。
- 定数の統合改善案 RR4(log P の 2 次近似剛性、SN/FAR 統合)は open(Sol 自信度 0.75)。

## 7. 版履歴

- v0.1(2026-08-09): 起草。予想 K2Q、退化テスト T1–T3(ε^{−4} 必要 + sharp 候補)、
  場合 (a)(b) 完全証明、場合 (c) の構造剛性(u‴ 明示式)と攻略アーキテクチャ。
- v0.2(2026-08-09): 場合 (c) の証明完成 — 予想を定理ドラフトに昇格(未レビュー)。
  核となる新要素: ①基本恒等式 e^{Δ³u} = Πρ₃(x_i)(有限差分による 2 次位相の厳密消去 —
  周波数非依存性の構造化)②剛性補題 R2(羃和 s₃, s₄ の同時消滅 ⇒ 零点は遠方)—
  「深い平坦性は零点を絶対距離 ≥ 3 へ押し出す」③超近接零点の平坦性下界(SN — Φ−1 の
  分子多項式の主係数が配置非依存 4/h⁵ になることから Chebyshev 下界で矛盾)。
  far/near の 2 分岐は「far で外挿・near で下界」として実現され、v0.1 の設計を踏襲。
- v0.3(2026-08-09): luna xhigh R1 の指摘を全受諾して修正 — [blocking] R2 吸収の定数連鎖
  (R₀ 10⁴→10⁶、c₁ 10⁻⁵⁸→10⁻⁶⁸、C₀ ≤ 10⁷⁰)/ [major] λ₄ 誤差定数 40→50(真の 5 次係数 48、
  n₄−d₄ = −6x⁴−48x³−140x²−176x−81 を明示)/ [major] ν = 0 の K2 帰着を証明分岐として明示 /
  [minor] D₂ の節点シフト計算・λ₄ 主枝の明記。Chebyshev 下界は複素根射影から
  「Re N の実主係数」経由に簡潔化(luna 提案採用)。
- v0.3.1(2026-08-09): **luna R2 = 受理可能判定(blocking/major ゼロ)**。FAR/SN/(c-ii)/λ₄ の
  全定数連鎖が再検算通過(SN の厳密値 η ≥ 4.21·10⁻⁶⁷ は本文の 3.5·10⁻⁶⁷ より安全側)。
  minor 3 件補記: 2πiℤ 分枝確定の一文(FAR)/ λ₄ 剰余の一様評価内訳 / T1 の切断変数(t-次数)。
  数値検証 9,568 配置・違反ゼロ(§5.4)。
- v0.4(2026-08-09): **Sol xhigh 研究協力の成果を検算の上採用**。①剛性補題 R2 → R2′
  (定数 4000 → 8、恒等式 1+r⁴ = r(1+r³)+(1−r) による初等証明)⇒ R₀ = 10³、c₁ = 10⁻⁴⁰、
  **C₀ ≤ 10⁴²(28 桁改善)** ②接続用の系 K2Q-wt(正規化版 — Λε 項が消える)/ K2Q-aff
  (affine 版)を §6.1 に追加 ③(W,d,Δ) 降下との接続仕様(L^∞ 明記・ε 読み替え・telescoping・
  W = 3 帳簿・B2 縮小)を §6.2 に、一般化射程を §6.4 に記録。v0.4 差分は luna R3 待ち。
