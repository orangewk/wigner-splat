# K(2)+1: exact pair-block kernel(木包絡)— wip

日付: 2026-08-09 / 著者: 本線 / status: **wip — v0.1: 因数分解補題 F2 証明済み・
K2QV への還元確立・数値検証(phantom 族の根治を含む)通過。残 = K2QV 本体の証明
(K2Q 場合 (c) の移植 — Sol 協力並走)**

> 位置づけ: [三原子一遷移補題](2026-08-09-three-atom-one-transition--wip.md) §3.7.4 の
> 残補題 2(Sol C.4 提案を検算の上採用)。pair を Taylor 置換せず exact block のまま保持する
> 新 kernel — phantom envelope 問題(§3.6.0 反例)を包絡の定義で根治し、(iii) 型証明書の
> 大部分を不要化することを狙う。kernel の親は
> [K2Q](2026-08-09-quadratic-phase-turan-K2Q-weight21--wip.md)。

## 1. 設定と主張

F = B₁₂ + c₃e^{q₃}、B₁₂(t) := c₁e^{q₁(t)} + c₂e^{q₂(t)}(c_j ≠ 0、q_j 相異なる複素 2 次)、
pair held: β_B := sup_K|η| ≤ 1/8(η := q₂ − q₁)。K 長さ 1、E ⊆ K 長さ ρ。
**木包絡**: U_T(t) := max(log|B₁₂(t)|, m₃(t))、m₃ := log|c₃| + Re q₃。
(U_T > −∞ 各点: B₁₂ の零点でも m₃ は有限。)

**予想 K(2)+1(v0.1)**: ‖e^{−U_T}F‖_{∞,K} ≤ C_T ρ⁻⁴ ‖e^{−U_T}F‖_{∞,E}(C_T 絶対定数)。

設計性質(いずれも定義から直ちに):
- **天井**: e^{−U_T}|F| ≤ 2 が **exact** に成立(|F| ≤ |B₁₂| + e^{m₃} ≤ 2e^{U_T})。
- **phantom の根治**: pair が深く相殺すれば log|B₁₂| がそのまま下がる — 包絡が虚偽に
  膨れない。§3.6.0 反例の摂動族(q₂ = q₁ + 1/10 + δt²、c₁ = −c₂e^{1/10})は
  δ = 10⁻¹〜10⁻⁸ で一様に比 ≈ 2·10⁻⁴·ρ⁻⁴(数値、§4)— U_F では 1/δ 発散だった。
- W = 3 帳簿の保存(pair は質量 2 の block のまま、Taylor 剰余を独立 weight に数えない)。

## 2. 補題 F2(pair block の厳密因数分解)【証明済み】

**補題 F2**: 上の設定で w を e^{w} = −c₁/c₂ かつ |Im(w − η(t_c))| ≤ π なる分枝
(t_c = E 中心)、z(t) := η(t) − w、E(z) := (e^z − 1)/z(E(0) = 1)とすると
  **Φ := c₁ + c₂e^{η} = c₂e^{w} · (η(t) − w) · E(z(t))**(恒等式)
であり、ν := Re w = log|c₁/c₂| による三分岐で次が成立:

- **(D1) ν ≤ −2(原子 2 支配)**: Re z ≥ 15/8 で
  Φ = c₂e^{η}·V₂、V₂ := 1 − e^{−z}、|V₂| ∈ [1 − e^{−15/8}, 1 + e^{−15/8}] ⊂ [0.84, 1.16]。
  B₁₂e^{−q₂} 側で読む: **deg 0 多項式 c₂ × 単位 V₂ × e^{q₂}**。
- **(D2) ν ≥ 2(原子 1 支配)**: Re z ≤ −15/8 で Φ = c₁·V₁、V₁ := 1 − e^{z}、
  |V₁| ∈ [0.84, 1.16]。**deg 0 多項式 c₁ × 単位 V₁ × e^{q₁}**。
- **(D3) |ν| < 2(相殺可能域)**: z(K) ⊆ B* := [−17/8, 17/8] × i[−(π+1/4), π+1/4]。
  E は B* 上零点なし(E の零点は 2πik、|2πk| ≥ 2π > π + 1/4)で
  **|E| ∈ [0.27, 3.5]、‖(log E)^{(j)}‖_{∞,B*} ≤ (0.8, 0.17, 0.09)(j = 1,2,3)**
  (数値評価、グリッド 401×601 — 解析的裏付け: |E| ≥ (1−e^{−17/8})/|z| ≥ 0.88/4.01、
  |E| ≤ (e^{17/8}−1)/(17/8) ≤ 3.5)。
  **P_Φ(t) := c₂e^{w}(η(t) − w) は真正な deg ≤ 2 多項式で、その零点 = Φ の零点**
  (η(t) = w の解、K 上高々 2 個)。Φ = P_Φ · V、V := E(z(t))。

*証明*: 恒等式は c₁ = −c₂e^{w} の代入(数値 5 配置 ≤ 6·10⁻¹⁶)。分枝の存在: η(K) は
半径 β_B ≤ 1/8 の円板内、Im w は 2π 刻みで選べるので |Im(w−η(t_c))| ≤ π にできる。
Im z の評価: |Im z| ≤ |Im(η − η(t_c))| + π ≤ 2β_B + π ≤ π + 1/4。
(D1): Re z = Re η − ν ≥ −1/8 + 2 − 1/8… 正しくは ≥ −β_B + 2 ≥ 15/8、
|e^{−z}| ≤ e^{−15/8} ≤ 0.154、Φ = c₂e^{η}(1 − e^{−z}) は恒等式の書換え。(D2) 対称。
(D3): |Re z| ≤ |ν| + β_B < 17/8 と Im 評価で z(K) ⊆ B*。E の非零性は零点の位置から。∎

**帰結(包絡の等価)**: 各分岐で U_T = max(log|P·V| + Re q_b, m₃)(q_b = 基準位相、
P = 該当多項式)は、単位の有界性より
  **U″ := max(log|P| + Re q_b, m₃) との差が一様有界**: |U_T − U″| ≤ max(|log 0.27|, log 3.5) ≤ 1.31
(D3; D1/D2 は ≤ 0.18)。bridge 損失 e^{2·1.31} ≤ 14 — **discount は絶対定数**
((iii) 型の非有界 Δ* はここでは発生しない — P_Φ が真の零点を持つため)。

## 3. K2QV への還元と移植分析

**定義(目標 kernel K2QV)**: F = P(t)V(t)e^{q_b} + c₃e^{q₃}(deg P ≤ 2、
V 零点なし、‖log V‖_{C³(K)} ≤ Λ_V 絶対定数)、包絡 U_V := max(log|PV| + Re q_b, m₃)。
主張: ‖e^{−U_V}F‖_K ≤ C(Λ_V)ρ⁻⁴‖e^{−U_V}F‖_E。

**還元**: F2 により K(2)+1 ⇐ K2QV(D3: Λ_V ≤ 1 の V = E∘z; D1/D2: deg 0 で
さらに単純)。z(t) 合成の C³ 評価: z′ = η′、z″ = η″、z‴ = 0 と Markov
(sup_K|η| ≤ 1/8 ⇒ |η′| ≤ 1, |η″| ≤ 2)で ‖log V‖_{C³} ≤ 0.8·1 + 0.17·(1+2) + 0.09·1 ≤ 1.4
級 — 絶対定数(粗い勘定、正確な帳簿は本証明で)。

**K2Q 証明の移植分析**(どこが運べてどこが新規か):
- **場合 (a)(b)(FAR 側含む)**: pointwise 性質 (P1)′–(P4)′ と包絡の max 構造のみ使用 —
  多項式性不使用。φ̃ := log|P̃V| + Re p に置換して**そのまま運べる見込み(低リスク)**。
- **場合 (c) の平坦性継承 (c1)(c2)**: osc 評価 — 同上、低リスク。
- **基本恒等式(K2Q §5.2)**: u := log(P̃Ve^{p}) に対し
  e^{Δ³u} = Πρ₃(x_i) · **e^{Δ³ log V}**、|Δ³ log V| ≤ h³‖(log V)‴‖ = O(h³) —
  有限差分恒等式は**乗法的 e^{O(h³)} 摂動付きで生存**。K2Q の後段(R2′ 羃和剛性、
  深平坦 ⇒ 零点遠方)は誤差付き恒等式で動く設計(λ₄ 剰余と同じ扱い)—
  閾値・定数の再検算が必要(**中リスク**)。
- **SN 分岐(超近接零点)**: K2Q は「分子多項式 N の実主係数が配置非依存 4/h⁵」という
  多項式構造 + Chebyshev 下界を使う。V の乗法摂動が N の主係数論法をどう変えるかは
  **要再導出(最高リスク箇所)**。代替: SN 域では |t−x_i| ≤ 3h で V の局所 osc が
  O(h) — 主係数摂動 O(h) なら Chebyshev 下界は生存するはず(未検証)。
- **D1/D2(deg 0)**: K2(純指数 2 原子)の単位付き移植 — (a)(b)(c) とも
  K2 の方が単純なので低リスク。

## 4. 数値検証(2026-08-09、scratchpad k2p1_numeric.py)

- E の B* 上界: |E| ∈ [0.2789, 3.4696]、(log E) の 1–3 階 ≤ (0.80, 0.17, 0.09)。
- 因数分解恒等式: 5 配置で相対誤差 ≤ 6·10⁻¹⁶。
- **phantom 摂動族の根治**: δ = 10⁻¹, 10⁻³, 10⁻⁵, 10⁻⁸ で比/ρ⁻⁴ = 2.0·10⁻⁴(一様)。
- ランダム分裂配置 3,000(near-phantom pair 1/3 混入、singleton 勾配 ~60 まで、
  c₃ スケール 10⁻⁸〜10²): **違反ゼロ**、最悪 比/ρ⁻⁴ = 2.5·10⁻⁴。
  (sharp 族は狙い撃ちが必要 — T1 型の検証は K2QV 本証明時に。)

## 5. 作業計画

1. ~~F2 因数分解 + 数値検証~~ **完了(§2, §4)**。
2. K2QV 本体の証明: (a)(b) 移植 → (c) 恒等式の摂動版 → SN 分岐の再導出(最高リスク)
   → D1/D2 の K2 移植。Sol xhigh に移植分析の検証 + SN 分岐の設計を依頼(並走)。
3. K(2)+1 確定後: 三原子 wrapper(§3.7)の再設計 — (iii) 証明書の消滅範囲の確定、
  bridge (DC) の残り(preparation lemma の守備範囲縮小)。
4. 敵対的レビュー(luna)→ 受諾判定。

## 6. 版履歴

- v0.1(2026-08-09): 起草。F2 因数分解補題(証明済み — 分枝選択 + D1/D2/D3 三分岐 +
  E の B* 上評価)、K2QV への還元、K2Q 移植分析(リスク階層: (a)(b) 低 / (c) 恒等式 中 /
  SN 分岐 高)、数値検証(phantom 根治・3,000 配置違反ゼロ)。
